{
  "name": "odbx",
  "version": "0.0.0",
  "private": true,
  "description": "Append-only persistence for canonical Oddo values",
  "type": "module",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
